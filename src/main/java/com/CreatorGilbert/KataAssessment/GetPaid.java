package com.CreatorGilbert.KataAssessment;

public class GetPaid {
    public static void main(String[] args){
        // BabySitter Sitter = new BabySitter();
        
    }
    
    
    /*
        private boolean verifyTime(int time, String AMorPM){
            // Would only allow times from 5 PM to 11PM and/or 12AM to 4AM
        }
    */
    
    /*
        private int getChangedTime(int time, String AMorPM){
            int ans = 0;
            if(AMorPM.equals("PM") || time == 12){
                ans = time - 5;
            }else{
                ans = time + 7;
            }
            return ans;
        }
    */
    /*
        private void askUserForTimes(){
            // set up input stream
            // set up output stream
            // out(Please enter the start time, whole number followed whether it's AM or PM \nExample: 4 PM)
            // out(input whole number: )
            // startNumber = in()
            // out(input AM or PM:)
            // startAMorPM = String.toUpperCase(in())
            // if(verifyTime(startNumber, startAMorPM))
            //      int startTime = getChangedTime(startNumber, startAMorPM);


            //This process would be followed 2 more times to get late time and bedtime
            //Then thrown into a SetTimes object
            
        }
    */

    
}
